<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Formulário de Perguntas</title>
    <style>
        /* Reset de estilos básicos */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            height: fit-content;
            font-family: Arial, sans-serif;
            background-color: #f0f8ff; /* Azul claro */
            color: #333;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }

        h1 {
            margin-bottom: 20px;
            color: #2a9df4; /* Azul mais escuro */
        }

        form {
            background-color: #ffffff; /* Fundo branco para o formulário */
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            width: 400px; /* Largura fixa do formulário */
        }

        fieldset {
            border: 1px solid #2a9df4; /* Bordas azul */
            margin-bottom: 15px;
            padding: 10px;
            border-radius: 4px;
        }

        legend {
            font-weight: bold;
            color: #2a9df4; /* Azul para o título do campo */
        }

        label {
            display: block;
            margin-bottom: 5px;
        }

        input[type="radio"] {
            margin-right: 10px; /* Espaçamento entre o rádio e o texto */
        }

        button {
            background-color: #2a9df4; /* Azul para o botão */
            color: white;
            padding: 10px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 16px;
            width: 100%; /* Largura total */
        }

        button:hover {
            background-color: #007bbf; /* Azul mais escuro ao passar o mouse */
        }
    </style>
</head>
<body>
    <h1>Quiz</h1>
    <form action="/submit" method="POST">
        {% for key, pergunta in perguntas %}
            <fieldset>
                <legend>{{ pergunta.pergunta }}</legend>
                {% for alternativa, texto in pergunta.alternativas %}
                    <label>
                        <input type="radio" name="q{{ key }}" value="{{ alternativa }}">
                        {{ texto }}
                    </label><br>
                {% endfor %}
            </fieldset>
        {% endfor %}
        <button type="submit">Enviar Respostas</button>
    </form>
</body>
</html>
